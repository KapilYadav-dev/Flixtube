<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.kay.flixtube">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <application
        android:name=".Utils.Application"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".UI.IntroUI.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UI.IntroUI.IntroActivity">

        </activity>
        <activity
            android:name=".UI.IntroUI.SignupActivity"
            android:fitsSystemWindows="true" />
        <activity
            android:name=".UI.IntroUI.LoginActivity"
            android:fitsSystemWindows="true" />
        <activity android:name=".UI.IntroUI.LandingActivity"></activity>
        <activity
            android:name=".UI.HomeUI.ViewAllActivity"
            android:fitsSystemWindows="true" />
        <activity
            android:name=".UI.HomeUI.PlayerActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:fitsSystemWindows="true"
            android:resizeableActivity="true"
            android:screenOrientation="landscape" />
        <activity
            android:name=".UI.HomeUI.DetailActivity"
            android:fitsSystemWindows="true" />
        <activity
            android:name=".UI.HomeUI.MainActivity"
            android:fitsSystemWindows="true" />
    </application>

</manifest>